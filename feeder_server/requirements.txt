# This requirements file is for the DeepHaven Python client, installed inside a Docker container.
websocket-client>=1.8.0
pyarrow>=14
duckdb>=1.3.2
requests>=2.31.0
tradingview-datafeed>=2.1.1
fastapi>=0.116.1
# Uvicorn with standard extras pulls in wsproto/uvloop, but we also add websockets explicitly for WS support
uvicorn[standard]>=0.35.0
websockets>=11,<13