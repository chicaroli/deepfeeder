[project]
name = "deepfeeder"
version = "1.1.0"
description = "DeepFeeder is a real-time market data feeder framework built on Deephaven"
readme = "README.md"
requires-python = ">=3.10"

[tool.uv.workspace]
members = ["feeder_client", "feeder_server"]

